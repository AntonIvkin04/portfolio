import { HeroAnimationDirective } from './hero-animation.directive';

describe('HeroAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
