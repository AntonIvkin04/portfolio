import { SectionHeightDirective } from './section-height.directive';

describe('SectionHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionHeightDirective();
    expect(directive).toBeTruthy();
  });
});
